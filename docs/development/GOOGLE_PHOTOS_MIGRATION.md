# Google Photos API 이미지 저장 마이그레이션 가이드

## ⚠️ 중요 제약사항

### Google Photos API의 한계
1. **무제한 저장 종료**: Google Photos의 무제한 저장은 **2021년 6월 1일부터 종료**되었습니다.
2. **Pixel 기기 혜택 종료**: Pixel 4a와 4xl도 더 이상 무제한 저장 혜택을 받지 못합니다.
3. **공개 URL 제한**: Google Photos Library API는 업로드된 이미지의 **직접 공개 URL을 제공하지 않습니다**.
4. **복잡한 인증**: OAuth 2.0 인증이 필요하며, 사용자별로 인증해야 합니다.
5. **API 제한**: Google Photos Library API는 주로 사진 관리용이며, 웹 애플리케이션의 이미지 호스팅에는 부적합합니다.

### ⚠️ Pixel 4a/4xl 무제한 저장 혜택 (사용자 기기)
**중요**: 사용자가 보유한 **Pixel 4a와 4xl**은 **고품질(High Quality) 무제한 업로드가 가능**합니다.

**제약사항:**
- ✅ **Pixel 4a/4xl 가능**: 고품질 업로드 무제한 (원본화질은 아님)
- ⚠️ **고품질 압축**: 원본화질이 아닌 고품질로 압축되어 저장 (대부분 시각적 차이 없음)
- ⚠️ **수동 업로드 필요**: Pixel 기기를 직접 사용하여 사진을 업로드해야 함
- ⚠️ **웹 API 불가**: 웹 애플리케이션에서 API로 직접 사용 불가능
- ⚠️ **공개 URL 미제공**: Google Photos API는 여전히 공개 URL을 제공하지 않음

**활용 방안:**
- Pixel 4a/4xl을 중간 서버로 활용하여 자동 백업 시스템 구축 가능
- 하이브리드 방식: Imgur API (즉시 사용) + Pixel 기기 (장기 백업)
- 자세한 설계는 `PIXEL_4A_4XL_IMAGE_STORAGE_DESIGN.md` 참조

**결론:**
- Pixel 4a/4xl을 백업 도구로 활용 가능 (Android 앱 개발 필요)
- 웹 애플리케이션의 즉시 이미지 저장소로는 여전히 부적합
- **현재는 Imgur API 사용, 필요시 Pixel 기기로 백업하는 하이브리드 방식 권장**

### 결론
**Google Photos API는 웹 애플리케이션의 이미지 저장소로 사용하기에 적합하지 않습니다.**
Pixel 1의 무제한 저장 혜택은 개인 백업용으로만 활용 가능하며, 웹 애플리케이션에서는 직접 활용할 수 없습니다.

## 🎯 추천 대안

### 옵션 1: Google Drive API (추천)
**장점:**
- 공개 URL 직접 제공 가능
- Google 계정의 15GB 무료 저장 공간 활용
- API가 간단하고 안정적
- 파일 관리가 용이

**단점:**
- 15GB 제한 (무료)
- 사용자별 OAuth 인증 필요

### 옵션 2: Imgur API (가장 간단)
**장점:**
- 완전 무료
- 공개 URL 즉시 제공
- OAuth 불필요 (API 키만으로 사용 가능)
- 구현이 매우 간단

**단점:**
- 무료 계정은 일일 업로드 제한 (1,250장/일)
- 이미지가 공개적으로 노출됨

### 옵션 3: Cloudinary (프로페셔널)
**장점:**
- 무료 티어 제공 (25GB 저장, 25GB 대역폭/월)
- 이미지 최적화 자동 제공
- CDN 포함
- 공개 URL 제공

**단점:**
- 무료 티어 제한
- 유료 전환 시 비용 발생

### 옵션 4: Vercel Blob Storage (현재 환경에 최적)
**장점:**
- Vercel 프로젝트와 완벽 통합
- 무료 티어 제공 (5GB 저장)
- 간단한 API
- CDN 포함

**단점:**
- Vercel 프로젝트에 한정
- 무료 티어 제한

## 📋 구현 계획

### Google Drive API 구현 (옵션 1)

#### 1. Google Cloud Console 설정
1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 새 프로젝트 생성 또는 기존 프로젝트 선택
3. "API 및 서비스" > "라이브러리"에서 "Google Drive API" 활성화
4. "사용자 인증 정보" > "OAuth 2.0 클라이언트 ID" 생성
5. 리다이렉트 URI 추가: `https://your-domain.com/auth/callback`

#### 2. 환경 변수 설정
```env
VITE_GOOGLE_CLIENT_ID=your-client-id
VITE_GOOGLE_CLIENT_SECRET=your-client-secret
```

#### 3. 코드 구현
- `src/services/googleDriveService.ts` 생성
- OAuth 2.0 인증 플로우 구현
- 이미지 업로드 및 공개 URL 생성

### Imgur API 구현 (옵션 2 - 가장 간단)

#### 1. Imgur 계정 및 API 키 발급
1. [Imgur API](https://api.imgur.com/oauth2/addclient) 접속
2. "Application" 타입 선택
3. Client ID 발급

#### 2. 환경 변수 설정
```env
VITE_IMGUR_CLIENT_ID=your-imgur-client-id
```

#### 3. 코드 구현
- `src/services/imgurService.ts` 생성
- 간단한 업로드 API 호출

## 💡 추천: Imgur API 구현 (기본값으로 설정됨)

가장 빠르고 간단하게 구현할 수 있으며, 무료로 사용 가능합니다.

**현재 상태:**
- ✅ Imgur API 서비스 구현 완료 (`src/services/imgurService.ts`)
- ✅ `imageService.ts`가 자동으로 Imgur API 사용 (기본값)
- ✅ 환경 변수로 Supabase Storage로 전환 가능

**사용 방법:**
1. `.env.local` 파일에 `VITE_IMGUR_CLIENT_ID=your-client-id` 추가
2. Imgur API 키 발급: https://api.imgur.com/oauth2/addclient
3. 코드 변경 없이 자동으로 Imgur API 사용

자세한 설정 방법은 `IMGUR_SETUP.md` 참조

