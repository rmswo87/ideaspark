# 필터링 기능 체계적 디버깅 체크리스트

## 문제 상황
- 로그상 필터는 작동함 (9개로 변경됨)
- setIdeas가 호출되고 있음
- 렌더링도 되고 있음
- 하지만 UI에는 여전히 동일한 결과가 표시됨

## 체계적 검증 체크리스트

### ✅ 1단계: 상태 업데이트 확인
- [ ] `setIdeas`가 실제로 호출되는가? → ✅ 확인됨 (로그에 있음)
- [ ] `ideas` 상태가 실제로 변경되는가? → ✅ 확인됨 (50개 → 9개)
- [ ] React DevTools에서 `ideas` 상태 변경을 확인할 수 있는가? → ⏳ 확인 필요

### ✅ 2단계: 리렌더링 확인
- [ ] `HomePage` 컴포넌트가 리렌더링되는가? → ✅ 확인됨 (로그에 있음)
- [ ] `ideas.map()`이 새로운 데이터로 실행되는가? → ✅ 확인됨 (로그에 있음)
- [ ] 각 `IdeaCard`가 새로운 `key`로 렌더링되는가? → ✅ 확인됨 (renderKey 변경됨)

### ⚠️ 3단계: 다른 곳에서 상태 덮어쓰기 확인 (중요!)
- [ ] `handleCollectIdeas`에서 `fetchIdeas()` 호출 시 필터가 무시되는가? → ⏳ 확인 필요
- [ ] 다른 `useEffect`에서 `setIdeas`를 호출하는가? → ⏳ 확인 필요
- [ ] 컴포넌트가 언마운트되고 다시 마운트되는가? → ⏳ 확인 필요
- [ ] `fetchIdeas`가 중복 호출되어 이전 데이터로 덮어쓰는가? → ⚠️ 의심됨!

### ⚠️ 4단계: useCallback 의존성 문제 (중요!)
- [ ] `fetchIdeas`의 의존성 배열에 `ideas.length`가 포함되어 무한 루프가 발생하는가? → ⚠️ 의심됨!
- [ ] `useEffect`가 `fetchIdeas` 변경 시마다 호출되어 이전 데이터로 덮어쓰는가? → ⚠️ 의심됨!

### 5단계: React 배치 업데이트 확인
- [ ] `setIdeas`와 `setLoading`이 동시에 호출되어 배치 처리되는가? → ⏳ 확인 필요
- [ ] `flushSync`를 사용하여 강제 동기 업데이트가 필요한가? → ⏳ 확인 필요

### 6단계: 실제 데이터 확인
- [ ] `getIdeas`가 실제로 다른 데이터를 반환하는가? → ✅ 확인됨 (9개)
- [ ] Supabase 쿼리가 실제로 필터를 적용하는가? → ✅ 확인됨 (로그에 있음)
- [ ] 네트워크 탭에서 API 응답을 확인할 수 있는가? → ⏳ 확인 필요

## 발견된 문제점

### 🔴 문제 1: useCallback 의존성에 `ideas.length` 포함
```typescript
const fetchIdeas = useCallback(async () => {
  // ...
}, [categoryFilter, subredditFilter, sortOption, debouncedSearchQuery, ideas.length]);
```
- `ideas.length`가 변경되면 `fetchIdeas`가 재생성됨
- `useEffect`가 `fetchIdeas` 변경 시마다 호출됨
- 이로 인해 무한 루프 또는 중복 호출 발생 가능

### 🔴 문제 2: useEffect가 fetchIdeas 변경 시마다 호출
```typescript
useEffect(() => {
  fetchIdeas();
}, [fetchIdeas])
```
- `fetchIdeas`가 변경될 때마다 호출됨
- `ideas.length`가 의존성에 포함되어 있으면 무한 루프 발생

## 해결 방법

### 방법 1: useCallback 의존성에서 `ideas.length` 제거
```typescript
const fetchIdeas = useCallback(async () => {
  // ideas.length를 사용하지 않도록 수정
}, [categoryFilter, subredditFilter, sortOption, debouncedSearchQuery]);
```

### 방법 2: useEffect 의존성을 직접 필터 값으로 변경
```typescript
useEffect(() => {
  fetchIdeas();
}, [categoryFilter, subredditFilter, sortOption, debouncedSearchQuery]);
```

## 다음 단계
1. useCallback 의존성에서 `ideas.length` 제거
2. useEffect 의존성을 직접 필터 값으로 변경
3. 테스트 및 검증

